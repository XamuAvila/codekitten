# Redis connection for BullMQ (dispatcher + worker)
REDIS_URL=redis://localhost:6379

# LLM provider key (v2+ — not used in v1 dry run)
ANTHROPIC_API_KEY=

# GitHub token for cloning private repos / PR API (v2+)
GITHUB_TOKEN=
