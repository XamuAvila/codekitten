# Docker Compose override for integration tests — isolated Redis on different port.
services:
  redis:
    ports:
      - "6382:6379"

  dispatcher:
    environment:
      - REDIS_URL=redis://redis:6379
      - PORT=3001
    ports:
      - "3002:3001"
